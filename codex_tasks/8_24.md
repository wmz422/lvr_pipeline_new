- 任务
    - 从/data/private/wmz/lvr_pipeline_new/runs/lam/v1/checkpoints/lam-epoch=003-step=00035000-val_loss=0.00000.ckpt恢复lam训练
        - 把8卡改成4卡调整梯度累计到一倍其他不变
        - 可以通过tensorboard看完整的训练曲线
    - 剩下4卡重新启动align阶段训练，是把 val_check_interval 调到 1000（约每 250 optimizer step 验证一次），同时显式保留 val/ce_loss_epoch top-3 + last.ckpt。每次验证保留一个ckp。
- 注意
    - 启动训练要保护进程